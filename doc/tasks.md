# 项目任务状态

本文档只记录**更贴近当前代码仓库真实状态**的进展。

## 已完成

### Web 化与基础闭环

* [x] 使用 `FastAPI` 搭建 Web 服务入口
* [x] 提供文件上传、状态查询、修订版下载接口
* [x] 前端页面支持上传、预览、审计卡片展示和下载
* [x] 使用 `UUID` 级目录隔离请求文件
* [x] 使用线程池承载同步审计任务

### 文档处理与修订

* [x] 使用 `python-docx` 读取 `.docx`
* [x] 通过 OpenXML 注入方式输出插入/删除修订痕迹
* [x] 将修订后的文档转换为 HTML 预览

### 规则审计

* [x] 中国手机号识别
* [x] 126/163 邮箱识别
* [x] 简单的签字职位缺失提示
* [x] 发票语句的固定补充逻辑
* [x] 指定违约金/罚息文本的删除逻辑
* [x] 特定争议解决标题下的固定替换逻辑

### 语义检索基础设施

* [x] `bge-m3` 向量召回能力
* [x] `bge-reranker-large` 重排能力
* [x] 本地模型目录和自动下载逻辑
* [x] 独立的文档切片能力

## 部分完成

* [~] 批注能力
  当前以可见的 `[批注: ...]` 插入为主，不是完整标准 Word comment
* [~] 文首文末审计
  当前只有非常轻量的 `Position/Title` 文本检查
* [~] 争议解决条款审计
  当前只在存在固定标题时替换，未覆盖“缺失则新增”等情况
* [~] 发票条款审计
  当前仅靠关键词与固定句判断，未真正识别付款结构

## 未完成

### 以 needs-260211.txt 为核心的业务需求

* [ ] 我方身份与称呼识别
* [ ] 邮件与合同一致性检查
* [ ] 中国人姓名签字识别
* [ ] 更可靠的职位信息判断
* [ ] 退款条款补充
* [ ] 银行账户变更条款补充
* [ ] 代扣税条款补充
* [ ] 争议解决条款缺失时的新增逻辑
* [ ] 更全面的罚息/违约金表达覆盖

### 主流程语义化

* [ ] 将 `DocProcessor` 接入主审计链路
* [ ] 将 `SemanticSearchEngine` 接入主审计链路
* [ ] 为各审计点建立查询、定位和命中策略
* [ ] 设计 `rule / semantic / llm` 三类审计来源标记

### 工程化

* [ ] 建立测试目录和基础测试样本
* [ ] 并发与压力测试
* [ ] 临时文件清理机制
* [ ] 更清晰的日志与错误上报
* [ ] 登录/历史记录/持久化
* [ ] 本地 LLM 接入
